/** CombineModule — en cours de construction (mission V2). */
import type { ModuleProps } from '../ModuleHost'

export function CombineModule(_props: ModuleProps) {
  return (
    <div className="flex items-center justify-center h-full text-forma-muted text-sm">
      Module Combine — en construction
    </div>
  )
}
